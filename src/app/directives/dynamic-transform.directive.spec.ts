import { DynamicTransformDirective } from './dynamic-transform.directive';

describe('DynamicTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTransformDirective();
    expect(directive).toBeTruthy();
  });
});
